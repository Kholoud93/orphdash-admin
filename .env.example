# Public runtime (optional)
NUXT_PUBLIC_SITE_URL=
# When `true`, `useDashboardAdminApi` calls `GET /api/admin-dashboard` (implement Nitro route before enabling).
NUXT_PUBLIC_DASHBOARD_API_ENABLED=false
